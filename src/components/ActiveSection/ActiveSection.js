import React, { Component } from 'react';
import s from './ActiveSection.module.scss';

class ActiveSection extends Component {
    render() {
        return (
            <div className={s.activeSectionContainer}>
                ActiveSection
            </div>
        );
    }
}

export default ActiveSection;
